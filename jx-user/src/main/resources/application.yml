server:
  port: 8080
spring:
  datasource:
    username: root
    password: liuyiwei1A
    #driver-class-name: com.mysql.cj.jdbc.Driver
    #url: jdbc:mysql://101.42.50.241:3306/ape-frame?serverTimeZone=Asia/shanghai&useUnicode=true&characterEncoding=UTF-8
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://101.42.50.241:3306/ape-frame?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=GMT%2B8
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 20
      min-idle: 20
      max-active: 100
      max-wait: 60000
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        login-username: admin
        login-password: 123456
      filter:
        stat:
          enabled: true
          log-slow-sql: true
          slow-sql-millis: 2000
        wall:
          enabled: true
sql:
  beauty:
    show: true

